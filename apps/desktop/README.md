# SkillOps Desktop

`apps/desktop` 是 SkillOps 的桌面客户端工程。

当前阶段目标：

- 提供一个桌面端聊天界面
- 支持用户输入消息并查看回复
- 预留本地工作目录选择能力
- 为后续接入 API Server、Agent Runtime 和本地模型服务打基础

当前这版优先实现客户端骨架和最小聊天主链路，不在这里引入复杂的插件、任务执行或多窗口能力。
