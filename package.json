{
  "name": "skillops",
  "version": "0.1.0",
  "private": true,
  "description": "SkillOps monorepo",
  "workspaces": [
    "apps/desktop"
  ],
  "scripts": {
    "dev:desktop": "npm run dev --workspace apps/desktop",
    "build:desktop": "npm run build --workspace apps/desktop"
  },
  "engines": {
    "node": ">=22.0.0"
  }
}
