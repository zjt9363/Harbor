{
  "name": "harbor",
  "version": "0.1.0",
  "private": true,
  "description": "Harbor monorepo",
  "workspaces": [
    "apps/desktop"
  ],
  "scripts": {
    "dev:desktop": "npm run dev --workspace apps/desktop",
    "build:desktop": "npm run build --workspace apps/desktop",
    "pack:desktop": "npm run pack --workspace apps/desktop",
    "dist:desktop:win": "npm run dist:win --workspace apps/desktop",
    "dist:desktop:portable": "npm run dist:portable --workspace apps/desktop",
    "package:desktop": "scripts\\package-desktop.bat"
  },
  "engines": {
    "node": ">=22.0.0"
  }
}
